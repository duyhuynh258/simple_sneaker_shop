import 'package:flutter/material.dart';

class ProductEmptyView extends StatelessWidget {
  const ProductEmptyView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
