import 'package:flutter/material.dart';

class ProductsLoadFailedView extends StatelessWidget {
  const ProductsLoadFailedView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
